{
  "name": "gestao-de-usuarios",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha test/**/*.js --reporter mocha-junit-reporter --reporter-options mochaFile=results.xml"    
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "mocha": "^11.7.5",
    "mocha-junit-reporter": "^2.2.1"
  }
}
